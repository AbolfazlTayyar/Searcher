# Elasticsearch connection URL
ES_HOST=http://localhost:9200

# Name of the Elasticsearch index holding indexed LinkedIn profiles
ES_INDEX_NAME=linkedin_profiles

# Allowed CORS origins for the API, as a JSON array of URLs
CORS_ORIGINS=["http://localhost:4173"]
